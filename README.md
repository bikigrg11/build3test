# build3test

# Kura Labs Build script 3

script1.sh - will allow you to create new user and initalize and add the git inforormation for the repository

script2.sh  - will allow the user to automatically add and commit the changes to git repository
            - will also check for any sensitive information
            - will push the code automatically to github after checking for sensitive information
